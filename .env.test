# Dummy values so the Supabase client can be constructed during `npm test`
# without hitting a real project. Tests should mock supabase calls.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
