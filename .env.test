# Dummy values so the Neon client can be constructed during `npm test`
# without hitting a real project. Tests mock the client anyway.
VITE_NEON_AUTH_URL=http://localhost:9999/neondb/auth
VITE_NEON_DATA_API_URL=http://localhost:9999/neondb/rest/v1
